# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
planner-config.json

# Generated by scripts/copy-sw.mjs (folder-sync SW served from app origin)
public/folder-sync/
public/diagnostics/

# Local tool artifacts
.playwright-mcp/
.claude/
*.png
region-dropdown.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# impeccable skill cache
.impeccable/

# Build artifact of scripts/spec/collect.mjs (regenerated every run).
spec-facts.json

# Build artifacts of scripts/spec/conflicts.mjs (regenerated every run). The
# workflow stages only docs/spec, so CI cannot commit these -- but a local run
# leaves them in the tree, and committing a file that is rewritten four times a
# day is pure churn.
spec-decisions.json
spec-decisions.md

# impeccable design-hook cache (local tool state)
.impeccable/
