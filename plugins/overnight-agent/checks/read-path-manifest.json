{
  "_comment": [
    "The file-layout contract (GH #292), in machine-readable form.",
    "",
    "One row per file the Overnight Agent reads or writes. Each row answers four",
    "questions: who OWNS the intent, who is allowed to WRITE it, whether it is on the",
    "PER-RUN READ PATH, and what BOUNDS its size.",
    "",
    "The invariant this file exists to enforce:",
    "",
    "    writer 'agent' + readPath 'every-run' is INVALID.",
    "",
    "That combination is the bug. It produced GH #262 on user-settings.md (grew to",
    "~947,000 chars, ~97% of the context window per call, runs stopped finishing) and",
    "GH #291 on the journals (task-400.md at 257 KB / ~66K tokens, read whole when",
    "planning that task). Declaring it as data means the third instance is refused",
    "before it is written rather than diagnosed after it breaks something.",
    "",
    "read-path-budget-sweep.mjs enforces this. It only ever reads."
  ],

  "tolerancePct": 15,

  "shrinkFactor": 0.5,
  "shrinkFactorNote": "g7, GH #382. A read-path file measuring below this fraction of its recorded baseline size is reported as SHRANK. SEPARATE FROM tolerancePct ON PURPOSE: tolerance is a drift question (is this creeping up?), while this is an order-of-magnitude one (did content just disappear?). Journals and settings files are append-mostly, so a HALVING is already extraordinary -- the event that motivated this lost 97% (202,489 -> 1,850 bytes, 41 agent turns and the user's own messages, when the planner app re-rendered a journal from its task description). Keep this LOOSE. A tight value turns a data-loss alarm into a routine-edit alarm, and this suite has twice been burned by a permanently-red line teaching every future run to skim it. Legitimate downward re-baselines are handled by the 'shrank' map in read-path-baseline.json, not by tuning this number.",

  "files": [
    {
      "id": "user-settings",
      "path": "{planner}/user-settings.md",
      "owner": "user",
      "writer": "app",
      "readPath": "every-run",
      "budgetKB": 120,
      "retention": "values only; the agent's prose belongs in agent-lore.md",
      "issue": 262,
      "note": "Writer is 'app' as the target state (GH #292). The agent appending here is what caused #262; SKILL.md now redirects those writes to agent-lore.md."
    },
    {
      "id": "skill",
      "path": "{skill}/SKILL.md",
      "owner": "repo",
      "writer": "developer",
      "readPath": "every-run",
      "budgetKB": 96,
      "retention": "reviewed edits only; guardrail prose splits per GH #127",
      "issue": 127,
      "note": "Developer-written, so it cannot grow unattended. Budgeted anyway because it is read in full on every run."
    },
    {
      "id": "agent-gate",
      "path": "{planner}/agent-gate.md",
      "owner": "user",
      "writer": "app",
      "readPath": "every-run",
      "budgetKB": 32,
      "retention": "a visible list the user edits; bounded by fitting on one screen",
      "issue": 288,
      "optional": true,
      "note": "Shipped by #290, and READ by the agent since #297 (`oa-state.ps1 gate`, PHASE 0). The agent only ever READS this file, which is what makes anything in it attributable to the user without a marker (#250) -- writer stays 'app' for that reason and must never become 'agent'. Budgeted tightly on purpose: its value is that the whole gate is reviewable at a glance, and a gate nobody reads is a gate nobody trusts."
    },
    {
      "id": "board",
      "path": "{planner}/planner.md",
      "owner": "user",
      "writer": "app",
      "readPath": "every-run",
      "budgetKB": 64,
      "retention": "completed rows move to planner-completed.md"
    },
    {
      "id": "agent-lore",
      "path": "{planner}/agent-lore.md",
      "owner": "agent",
      "writer": "agent",
      "readPath": "on-demand",
      "budgetKB": null,
      "retention": "needs a rotation policy (GH #104)",
      "issue": 104,
      "note": "Agent-written, so it MUST stay off the per-run read path. Unbudgeted on purpose: it is grepped, never loaded. If this ever becomes 'every-run' the contract check fails, which is the point."
    },
    {
      "id": "journals",
      "glob": "{planner}/journal/task-*.md",
      "idPattern": "^task-\\d+\\.md$",
      "strayPattern": "-(DESKTOP|LAPTOP)-[A-Z0-9]+\\.md$",
      "owner": "shared",
      "writer": "agent+user",
      "readPath": "every-run",
      "budgetKB": 64,
      "retention": "none today - file growth is still unbounded (GH #123 / #104)",
      "issue": 291,
      "boundedReader": {
        "command": "oa-state.ps1 extract -Id <id>",
        "ceilingKB": 24,
        "measuredMaxKB": 15.5,
        "measuredAt": "2026-09-01",
        "verifiedFiles": 239,
        "note": "The READ PATH is now bounded (GH #291). SKILL.md no longer says to read a linked journal in full; it says to run `extract`, which emits the user's framing, every message the user wrote (newest first), the agent's newest turn, the open asks and any unanswered reply -- each region capped, every elision stated in bytes, every emitted byte a VERBATIM substring of the source. Measured across all 239 live journals: 4.01 MB of source reduced to 1.16 MB of extract, worst single file 272 KB -> 15.5 KB, all 239 pass `-Verify`, none over the ceiling. It is read-only, so `oa-state.ps1 scan` returned a byte-identical (SHA-256) result before and after 239 extract runs -- the reopen hash and the consent attribution cannot move."
      },
      "contractException": {
        "issue": 291,
        "reason": "The READ-PATH half of this violation is closed: journals are now read through the bounded `extract` reader above, so planning a task costs at most ~24 KB regardless of journal size. The exception REMAINS because the other half is still open -- the files themselves still grow without bound (no retention policy, GH #123/#104), and this row stays budgeted so that growth keeps failing. Do not flip readPath away from 'every-run' to clear this: that would exempt the row from g2/g4 entirely and delete the growth signal, which is the one live alarm on these files."
      },
      "note": "SKILL.md says to read a task's linked journal(s) in full before planning. 95th percentile is ~41 KB, so 64 KB is above normal and flags genuine outliers rather than routine files.",
      "idPatternNote": "A real journal is task-<id>.md. The glob also matches OneDrive sync-conflict copies (task-463-DESKTOP-P9116M3.md), which no run reads -- oa-state.ps1 scan returned exactly 239 rows on 2026-09-01, the same count as files matching this pattern. Declaring the read-path SHAPE is deliberate: matching the conflict shape instead would need a guess per sync vendor and would miss a custom machine name.",
      "strayPatternNote": "SEPARATE FROM idPattern ON PURPOSE, and the separation is load-bearing. idPattern decides what is MEASURED; strayPattern decides what is ACCUSED. The folder also holds 153 legitimate deliverables that SKILL.md tells the agent to write as task-<id>-<slug>.md, and a conflict copy has the same shape as one. Collapsing the two would print 'remedy is to remove it' against the user's own deliverables. Files matching neither pattern are counted as companions and nothing is claimed about them. Keep this pattern NARROW: too narrow leaves a conflict copy uncounted, too wide accuses a deliverable, and those costs are not symmetric. Extend it only for a naming form actually observed on disk."
    }
  ]
}
